@model IEnumerable<GameInLibrary>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IgdbGameId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateAddedToLibrary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentUserStoryMission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentUserStoryProgressPercent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IgdbGameId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateAddedToLibrary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentUserStoryMission)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentUserStoryProgressPercent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
